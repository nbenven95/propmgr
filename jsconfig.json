/* Provides intellisense hints for esm-module-alias import statements (see "alias" in package.json) */
{
  "compilerOptions": {
    "paths": {
      "@*": ["./backend/*", "./frontend/src/*"]
    }
  }
}
