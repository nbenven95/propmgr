{
  "name": "propmgr",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "NODE_ENV=development nodemon --loader=./scripts/loader.mjs --no-warnings backend/server.js",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.0.1",
    "dotenv-expand": "^12.0.2",
    "esm-module-alias": "^2.2.1",
    "express": "^5.1.0",
    "mongoose": "^8.16.1",
    "multer": "^2.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "exports": "./backend/server.js",
  "type": "module",
  "engines": {
    "node": ">=14.16"
  },
  "aliases": {
    "@root": ".",
    "@config": "./backend/config",
    "@controllers": "./backend/controllers",
    "@models": "./backend/models",
    "@routes": "./backend/routes",
    "@util": "./backend/util"
  }
}
